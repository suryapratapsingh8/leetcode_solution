class Solution:
    def countBits(self, n: int) -> List[int]:
        a=[]
        for i in range (n+1):
            k=bin(i)
            y=k.count('1')
            a.append(y)
        return a

        
